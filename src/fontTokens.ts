/**
 * Local brand face — keep in sync with @font-face in `src/index.css` and `/public/fonts/Hamon-*.otf`.
 * Use weights 300 (Light), 400 (Regular .otf), 700 (Bold).
 */
export const FONT_HAMON = "'Hamon', system-ui, sans-serif";
